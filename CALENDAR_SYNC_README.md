# 📅 Синхронизация календаря литературных дат

## Обзор

Есть **два способа** работы с календарем:

### Способ 1: Ручное добавление (текущий)
- ✅ Вы добавляете события в Яндекс Календарь вручную
- ✅ Полный контроль над содержимым
- ❌ Требует времени на добавление

### Способ 2: Автоматическая синхронизация (новый)
- ✅ Автоматически создает события из данных API
- ✅ Всегда актуальные данные
- ✅ Можно запускать периодически

---

## 🔄 Автоматическая синхронизация

### Как это работает

1. **Скрипт `sync_calendar_from_api.py`** получает всех авторов из GraphQL API
2. Извлекает даты рождения (`birthday`) и смерти (`deathday`)
3. Создает события для календаря
4. Сохраняет в файл для импорта в Яндекс Календарь

### Установка и запуск

1. **Установите зависимости:**
```bash
pip install aiohttp
```

2. **Настройте скрипт:**

Откройте `sync_calendar_from_api.py` и укажите URL API:

```python
GRAPHQL_ENDPOINT = "https://ваш-api-endpoint.com/graphql"
```

3. **Запустите синхронизацию:**

```bash
python sync_calendar_from_api.py
```

4. **Импортируйте в Яндекс Календарь:**

- Откройте [Яндекс Календарь](https://calendar.yandex.ru)
- Создайте новый календарь "Литературные даты"
- Импортируйте файл `literary_calendar_export.txt`
- Или скопируйте события вручную

### Автоматический запуск

Можно настроить автоматический запуск раз в месяц:

**Windows (Task Scheduler):**
1. Откройте Планировщик заданий
2. Создайте новое задание
3. Установите запуск `python sync_calendar_from_api.py` раз в месяц

**Linux (cron):**
```bash
# Добавьте в crontab (запуск 1 числа каждого месяца в 00:00)
0 0 1 * * /usr/bin/python3 /path/to/sync_calendar_from_api.py
```

---

## 📋 Формат событий

Скрипт создает события в таком формате:

```
# Антон Чехов родился в 1860 году
29 января 2026 00:00
https://svetapp.rusneb.ru/catalog?authors=c52f926c-dde3-4631-b1cf-4a8849ad5be9&page=1
День рождения Антон Чехов

Книги Антон Чехов в приложении «Свет»
https://svetapp.rusneb.ru/catalog?authors=c52f926c-dde3-4631-b1cf-4a8849ad5be9&page=1
```

---

## ⚙️ Настройки

### Формат дат в API

Скрипт поддерживает разные форматы дат:
- `"1905"` - только год
- `"1905-01-01"` - полная дата
- `"01.01.1905"` - дата с точками

### Фильтрация авторов

По умолчанию скрипт берет только активных авторов (`isActive: true`).

Чтобы изменить, отредактируйте в `sync_author_events()`:

```python
if not author.get('isActive', True):  # Измените условие
    continue
```

### Типы событий

Скрипт создает два типа событий:
- **Дни рождения** - из поля `birthday`
- **Дни памяти** - из поля `deathday`

Чтобы отключить один из типов, закомментируйте соответствующий блок в коде.

---

## 🔗 Интеграция с ботом

После синхронизации:

1. **Экспортируйте календарь из Яндекс:**
   - Откройте настройки календаря
   - Скопируйте URL экспорта (как в вашем примере)

2. **Используйте URL в боте:**
   - Вставьте URL в `literary_calendar_bot_config.py`
   - Бот будет использовать обновленный календарь

---

## 💡 Рекомендации

### Гибридный подход

Можно комбинировать оба способа:

1. **Автоматически** добавлять дни рождения авторов из API
2. **Вручную** добавлять специальные события:
   - Праздники (День библиотек, День книги и т.д.)
   - Юбилеи книг
   - Специальные даты

### Периодичность синхронизации

Рекомендуется синхронизировать:
- **Раз в месяц** - для обновления новых авторов
- **При добавлении новых авторов** - для актуальности

### Резервное копирование

Перед синхронизацией сохраняйте текущий календарь:
- Экспортируйте календарь в файл
- Храните несколько версий

---

## 🐛 Решение проблем

### События не создаются

- ✅ Проверьте подключение к API
- ✅ Убедитесь, что у авторов заполнены поля `birthday`/`deathday`
- ✅ Проверьте формат дат в API

### Неправильные даты

- ✅ Проверьте формат дат в API
- ✅ Убедитесь, что даты в правильном формате (год, полная дата)

### Дубликаты событий

- ✅ Перед импортом проверьте файл на дубликаты
- ✅ Используйте функцию поиска в Яндекс Календаре перед добавлением

---

## 📚 Дополнительные возможности

### Добавление других событий

Можно расширить скрипт для добавления:
- Юбилеев книг (по дате публикации)
- Событий по тегам
- Событий по коллекциям

### Интеграция с Яндекс Calendar API

Для полной автоматизации можно использовать [Яндекс Calendar API](https://yandex.ru/dev/calendar/):
- Автоматическое добавление событий
- Обновление существующих событий
- Удаление устаревших событий

---

## 📝 Пример использования

```bash
# 1. Запустите синхронизацию
python sync_calendar_from_api.py

# 2. Проверьте файл
cat literary_calendar_export.txt

# 3. Импортируйте в Яндекс Календарь
# (через веб-интерфейс)

# 4. Обновите URL в настройках бота
# (скопируйте новый URL экспорта)

# 5. Перезапустите бота
python run_bot.py
```

---

**Итого:** Вы можете использовать **оба подхода** - автоматическую синхронизацию для массовых событий и ручное добавление для специальных дат! 🎉

