# 🚀 Руководство по размещению Telegram-бота

## Обзор вариантов размещения

### 🆓 Бесплатные варианты
1. **Heroku** (ограниченный бесплатный план)
2. **Railway** (бесплатный план с ограничениями)
3. **Render** (бесплатный план)
4. **PythonAnywhere** (бесплатный план)
5. **VPS** (бесплатные варианты)

### 💰 Платные варианты
1. **VPS** (DigitalOcean, Vultr, Hetzner)
2. **AWS/GCP/Azure** (облачные сервисы)
3. **Docker + любой хостинг**

---

## 🆓 Бесплатные варианты (рекомендуемые)

### 1. Railway.app ⭐ (Рекомендуется)

**Плюсы:**
- ✅ Бесплатный план с $5 кредитами в месяц
- ✅ Простая настройка через GitHub
- ✅ Автоматический деплой
- ✅ Поддержка Python

**Как разместить:**

1. **Создайте аккаунт:**
   - Зайдите на [railway.app](https://railway.app)
   - Войдите через GitHub

2. **Подготовьте проект:**
   - Создайте файл `Procfile`:
   ```
   worker: python run_bot.py
   ```
   
   - Создайте файл `runtime.txt`:
   ```
   python-3.11.0
   ```

3. **Добавьте переменные окружения:**
   - В Railway: Settings → Variables
   - Добавьте:
     ```
     BOT_TOKEN=ваш_токен
     GRAPHQL_ENDPOINT=ваш_api_url
     GROUP_CHAT_ID=id_группы
     CALENDAR_URL=url_календаря
     ```

4. **Деплой:**
   - Подключите GitHub репозиторий
   - Railway автоматически задеплоит

**Стоимость:** Бесплатно (до $5/месяц использования)

---

### 2. Render.com ⭐

**Плюсы:**
- ✅ Бесплатный план
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка

**Как разместить:**

1. **Создайте аккаунт на [render.com](https://render.com)**

2. **Создайте новый Web Service:**
   - Connect GitHub репозиторий
   - Build Command: `pip install -r requirements_bot.txt`
   - Start Command: `python run_bot.py`

3. **Добавьте переменные окружения:**
   - Environment → Add Environment Variable
   - Добавьте все необходимые переменные

4. **Настройте как Background Worker:**
   - В настройках выберите "Background Worker" вместо "Web Service"

**Стоимость:** Бесплатно (с ограничениями)

---

### 3. PythonAnywhere

**Плюсы:**
- ✅ Бесплатный план
- ✅ Специально для Python
- ✅ Простая настройка

**Как разместить:**

1. **Создайте аккаунт на [pythonanywhere.com](https://www.pythonanywhere.com)**

2. **Загрузите файлы:**
   - Files → Upload files
   - Загрузите все файлы бота

3. **Настройте задачу:**
   - Tasks → Create new task
   - Command: `python3.10 /home/ваш_username/literary_calendar_bot.py`
   - Schedule: Daily at 9:00 AM

4. **Настройте переменные окружения:**
   - Files → bashrc
   - Добавьте:
   ```bash
   export BOT_TOKEN="ваш_токен"
   export GRAPHQL_ENDPOINT="ваш_api_url"
   export GROUP_CHAT_ID="id_группы"
   ```

**Стоимость:** Бесплатно (с ограничениями)

**Ограничения:**
- ❌ Задачи запускаются только в определенное время
- ❌ Нельзя держать процесс постоянно запущенным

---

### 4. Heroku (ограниченный бесплатный план)

**Плюсы:**
- ✅ Популярная платформа
- ✅ Хорошая документация

**Минусы:**
- ❌ Убрали бесплатный план (только платный)
- ❌ Требует кредитную карту

**Стоимость:** От $7/месяц

---

## 💰 Платные варианты (для продакшена)

### 1. VPS (DigitalOcean, Vultr, Hetzner)

**Рекомендуется для продакшена!**

**Плюсы:**
- ✅ Полный контроль
- ✅ Низкая стоимость ($5-10/месяц)
- ✅ Надежность

**Как разместить:**

#### Шаг 1: Создайте VPS

**DigitalOcean:**
- Зайдите на [digitalocean.com](https://www.digitalocean.com)
- Создайте Droplet (Ubuntu 22.04)
- Минимальная конфигурация: $6/месяц

**Hetzner (дешевле):**
- Зайдите на [hetzner.com](https://www.hetzner.com)
- Cloud → Create Server
- Минимальная конфигурация: €4/месяц

#### Шаг 2: Настройте сервер

```bash
# Подключитесь к серверу
ssh root@ваш_ip

# Обновите систему
apt update && apt upgrade -y

# Установите Python
apt install python3 python3-pip git -y

# Клонируйте репозиторий
git clone https://github.com/ваш_username/literary-calendar-bot.git
cd literary-calendar-bot

# Установите зависимости
pip3 install -r requirements_bot.txt
```

#### Шаг 3: Настройте systemd сервис

Создайте файл `/etc/systemd/system/literary-bot.service`:

```ini
[Unit]
Description=Literary Calendar Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/literary-calendar-bot
Environment="BOT_TOKEN=ваш_токен"
Environment="GRAPHQL_ENDPOINT=ваш_api_url"
Environment="GROUP_CHAT_ID=id_группы"
Environment="CALENDAR_URL=url_календаря"
ExecStart=/usr/bin/python3 /root/literary-calendar-bot/run_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### Шаг 4: Запустите сервис

```bash
# Перезагрузите systemd
systemctl daemon-reload

# Включите автозапуск
systemctl enable literary-bot

# Запустите бота
systemctl start literary-bot

# Проверьте статус
systemctl status literary-bot
```

#### Шаг 5: Настройте логи

```bash
# Просмотр логов
journalctl -u literary-bot -f

# Логи за последний час
journalctl -u literary-bot --since "1 hour ago"
```

**Стоимость:** $5-10/месяц

---

### 2. Docker + любой хостинг

**Плюсы:**
- ✅ Изоляция окружения
- ✅ Легкое развертывание
- ✅ Работает везде

**Создайте Dockerfile:**

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements_bot.txt .
RUN pip install --no-cache-dir -r requirements_bot.txt

COPY . .

CMD ["python", "run_bot.py"]
```

**Создайте docker-compose.yml:**

```yaml
version: '3.8'

services:
  bot:
    build: .
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - GRAPHQL_ENDPOINT=${GRAPHQL_ENDPOINT}
      - GROUP_CHAT_ID=${GROUP_CHAT_ID}
      - CALENDAR_URL=${CALENDAR_URL}
```

**Запуск:**

```bash
docker-compose up -d
```

---

## 🔧 Настройка переменных окружения

Вместо конфигурационного файла используйте переменные окружения:

### Обновите `run_bot.py`:

```python
import os

BOT_TOKEN = os.getenv('BOT_TOKEN')
GRAPHQL_ENDPOINT = os.getenv('GRAPHQL_ENDPOINT')
GROUP_CHAT_ID = os.getenv('GROUP_CHAT_ID')
CALENDAR_URL = os.getenv('CALENDAR_URL', 'https://calendar.yandex.ru/export/html.xml?private_token=...')
```

---

## 📋 Чеклист перед деплоем

- [ ] Все зависимости в `requirements_bot.txt`
- [ ] Переменные окружения настроены
- [ ] Логирование настроено
- [ ] Обработка ошибок добавлена
- [ ] Тесты пройдены (`python test_bot.py`)

---

## 🎯 Рекомендации по выбору

### Для тестирования:
- ✅ **Railway** или **Render** (бесплатно, просто)

### Для продакшена:
- ✅ **VPS** (DigitalOcean/Hetzner) - лучший вариант
- ✅ **Docker** на любом хостинге

### Для небольшого проекта:
- ✅ **PythonAnywhere** (бесплатно, но с ограничениями)

---

## 🔒 Безопасность

### Никогда не коммитьте:
- ❌ Токены ботов
- ❌ API ключи
- ❌ ID групп
- ❌ Приватные URL

### Используйте:
- ✅ Переменные окружения
- ✅ `.env` файл (добавьте в `.gitignore`)
- ✅ Секреты в настройках хостинга

### Создайте `.gitignore`:

```
.env
*.pyc
__pycache__/
*.log
literary_calendar_bot_config.py
```

---

## 📊 Мониторинг

### Логирование

Бот уже настроен на логирование. Для продакшена добавьте:

```python
import logging
from logging.handlers import RotatingFileHandler

handler = RotatingFileHandler(
    'bot.log',
    maxBytes=10485760,  # 10MB
    backupCount=5
)
handler.setFormatter(logging.Formatter(
    '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
))
logger.addHandler(handler)
```

### Мониторинг здоровья

Добавьте простой health check:

```python
async def health_check():
    """Проверка работоспособности бота"""
    try:
        await bot.get_me()
        return True
    except:
        return False
```

---

## 🚨 Решение проблем

### Бот не запускается

1. Проверьте логи:
   ```bash
   journalctl -u literary-bot -n 50
   ```

2. Проверьте переменные окружения:
   ```bash
   env | grep BOT_TOKEN
   ```

3. Проверьте подключение к API:
   ```bash
   curl $GRAPHQL_ENDPOINT
   ```

### Бот падает

1. Проверьте логи на ошибки
2. Убедитесь, что все зависимости установлены
3. Проверьте доступность API и календаря

### Бот не отправляет сообщения

1. Проверьте токен бота
2. Проверьте ID группы
3. Убедитесь, что бот добавлен в группу

---

## 📚 Полезные ссылки

- [Railway.app](https://railway.app)
- [Render.com](https://render.com)
- [PythonAnywhere](https://www.pythonanywhere.com)
- [DigitalOcean](https://www.digitalocean.com)
- [Hetzner Cloud](https://www.hetzner.com/cloud)

---

**Рекомендация:** Начните с **Railway** или **Render** для тестирования, затем перейдите на **VPS** для продакшена! 🚀

