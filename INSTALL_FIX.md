# 🔧 Исправление проблемы с установкой зависимостей

## Проблема

При установке `aiohttp==3.9.1` на Windows возникает ошибка:
```
error: Microsoft Visual C++ 14.0 or greater is required
```

Это происходит потому, что старая версия `aiohttp` требует компиляции C расширений.

## ✅ Решение

Я заменил `aiohttp` на `httpx`, который:
- ✅ Не требует компиляции C расширений
- ✅ Уже установлен как зависимость `python-telegram-bot`
- ✅ Имеет похожий API
- ✅ Работает на всех платформах

## 📋 Что изменено

1. **`requirements_bot.txt`** - заменен `aiohttp` на `httpx`
2. **`literary_calendar_bot.py`** - обновлен код для использования `httpx`
3. **`sync_calendar_from_api.py`** - обновлен код для использования `httpx`
4. **`test_bot.py`** - обновлен код для использования `httpx`

## 🚀 Установка

Теперь просто выполните:

```bash
pip install -r requirements_bot.txt
```

Должно установиться без ошибок! ✅

## 🔍 Проверка

После установки проверьте:

```bash
python test_bot.py
```

Если все работает - отлично! 🎉

## 📝 Альтернативное решение (если нужно использовать aiohttp)

Если по какой-то причине нужно использовать именно `aiohttp`, установите Microsoft C++ Build Tools:

1. Скачайте: https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. Установите "Desktop development with C++"
3. Переустановите зависимости

Но `httpx` работает отлично и не требует дополнительных инструментов! 👍

