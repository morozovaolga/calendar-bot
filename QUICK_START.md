# 🚀 Быстрый старт бота литературного календаря

## Шаг 1: Установка зависимостей

```bash
pip install python-telegram-bot aiohttp
```

## Шаг 2: Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Придумайте имя и username для бота
4. **Скопируйте токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 3: Добавление бота в группу

1. Создайте группу в Telegram или используйте существующую
2. Добавьте вашего бота в группу (через поиск по username)
3. Сделайте бота администратором (опционально, но рекомендуется)

## Шаг 4: Получение ID группы

**Способ 1 (простой):**
1. Добавьте [@userinfobot](https://t.me/userinfobot) в группу
2. Он покажет ID группы

**Способ 2:**
1. Добавьте [@RawDataBot](https://t.me/RawDataBot) в группу
2. Отправьте любое сообщение
3. Бот покажет JSON с `chat.id` - это и есть ID группы

**Способ 3 (через API):**
1. Отправьте сообщение в группу
2. Откройте `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
3. Найдите `"chat":{"id":-1001234567890}` - это ID группы

## Шаг 5: Настройка конфигурации

Откройте `literary_calendar_bot_config.py` и заполните:

```python
BOT_TOKEN = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"  # Ваш токен
GRAPHQL_ENDPOINT = "https://ваш-api.com/graphql"     # URL API
GROUP_CHAT_ID = "-1001234567890"                     # ID группы
```

**Важно:** Узнайте URL GraphQL API у администраторов или найдите в документации проекта.

## Шаг 6: Тестирование

Сначала протестируйте парсинг календаря:

```bash
python test_bot.py
```

Это проверит:
- ✅ Загрузку календаря
- ✅ Парсинг событий
- ✅ Форматирование сообщений

## Шаг 7: Запуск бота

### Вариант A: Ручной запуск (для тестирования)

Создайте файл `run_bot.py`:

```python
from literary_calendar_bot import LiteraryCalendarBot
import asyncio
import literary_calendar_bot_config as config

async def main():
    bot = LiteraryCalendarBot(
        bot_token=config.BOT_TOKEN,
        calendar_url=config.CALENDAR_URL,
        graphql_endpoint=config.GRAPHQL_ENDPOINT,
        group_chat_id=config.GROUP_CHAT_ID
    )
    
    # Отправляем события на сегодня (для теста)
    await bot.send_daily_digest()
    
    # Или запускаем ежедневную рассылку
    # await bot.run_daily()

if __name__ == "__main__":
    asyncio.run(main())
```

Запустите:
```bash
python run_bot.py
```

### Вариант B: Автоматическая ежедневная рассылка

Измените `run_bot.py`:

```python
# Запускаем ежедневную рассылку
await bot.run_daily()
```

Бот будет проверять календарь каждый день в 9:00 и отправлять события.

## 🎯 Что делает бот?

1. **Каждый день в 9:00** (можно изменить) проверяет календарь
2. **Находит события** на сегодняшний день
3. **Ищет книги** авторов через GraphQL API
4. **Форматирует красивое сообщение** с эмодзи и ссылками
5. **Отправляет в группу** Telegram

## 📝 Пример сообщения

```
📚 Антон Чехов родился в 1860 году

📅 29 января 2026

Книги Чехова в приложении

📖 Книги в приложении «Свет»:
• Вишневый сад
• Дама с собачкой
• Палата №6

🔗 Ссылки:
Открыть в каталоге
```

## ⚙️ Настройка времени отправки

В `literary_calendar_bot.py` найдите строку:

```python
if now.hour == 9 and now.minute == 0:
```

Измените `9` на нужный час (0-23).

## 🐛 Решение проблем

### Бот не отвечает

- ✅ Проверьте, что бот добавлен в группу
- ✅ Проверьте токен бота
- ✅ Проверьте ID группы

### Ошибки парсинга календаря

- ✅ Проверьте, что URL календаря доступен
- ✅ Запустите `test_bot.py` для диагностики

### Ошибки API

- ✅ Проверьте URL GraphQL API
- ✅ Убедитесь, что API доступен
- ✅ Проверьте формат запросов

## 📚 Дополнительная информация

Полная документация: `README_BOT.md`

## 💡 Идеи для улучшения

- Добавить команду `/today` для ручной отправки
- Добавить команду `/tomorrow` для предпросмотра завтрашних событий
- Добавить фильтры по типам событий
- Добавить уведомления о предстоящих событиях

Удачи! 🚀

