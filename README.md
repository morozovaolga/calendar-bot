# Telegram-бот литературного календаря

Бот для ежедневной рассылки литературных дат с ссылками на книги и авторов.

## 🎯 Функционал

- 📅 Загружает события из базы данных `literary_events.db`
- 📚 Автоматически находит книги авторов через интеграцию с каталогом
- 🔗 Формирует красивые сообщения со ссылками на книги и обложками
- 📖 Хранит события, авторов, книги, теги и категории в БД
- ⏰ Отправляет события по запросу через команды Telegram
- 🎨 Форматирует сообщения с эмодзи, ссылками и метаданными о книгах
- 🌐 Веб-интерфейс для управления событиями и ссылками

## 🗄️ Архитектура данных

### База данных (`literary_events.db`)

Структура SQLite БД:

- **events**: События (дни рождения писателей, знаменательные даты)
  - `event_date`: дата в формате MM-DD (ежегодная)
  - `event_type`: 'birthday', 'death', 'book_published', 'memorable_day'
  - `title`: название события
  - `description`: описание
  - `author_name`, `book_title`, `year`: доп. информация

- **event_references**: Ссылки на ресурсы (многие-ко-многим)
  - `reference_type`: 'author', 'book', 'tag', 'category', 'film', 'article'
  - `reference_uuid`: UUID в API (автор, книга, категория)
  - `reference_slug`: URL slug для книги
  - `reference_name`: название для отображения
  - `metadata`: JSON с обложкой, аннотацией и т.д.

## 📋 Требования

- Python 3.8+
- Telegram бот (токен от [@BotFather](https://t.me/BotFather))

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/yourusername/calendar-bot.git
cd calendar-bot
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

> Для разработки можно поставить дев-зависимости:
> `pip install -r requirements-dev.txt`
>
> Если нужна максимально воспроизводимая среда “как у автора”, используйте снимок:
> `pip install -r requirements.lock.txt`

### 3. Настройте окружение

Скопируйте шаблон конфигурации:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните необходимые значения:

```
BOT_TOKEN=ваш_токен_от_botfather
DB_PATH=./literary_events.db
SEND_HOUR=9
TIMEZONE=Europe/Moscow
```

### 4. Получите токен бота у @BotFather

- Откройте [@BotFather](https://t.me/BotFather)
- Отправьте `/newbot`
- Следуйте инструкциям
- Скопируйте полученный токен в файл `.env`

### 5. Запустите бота

**Linux/macOS:**

```bash
export BOT_TOKEN='ваш_токен_от_botfather'
python run_bot.py
```

**Windows PowerShell:**

```powershell
$env:BOT_TOKEN = 'ваш_токен_от_botfather'
python run_bot.py
```

**С файлом .env (все платформы):**

```bash
python run_bot.py
```

Бот запустится и будет ждать команд от пользователей.

## 💻 Использование

### Команды бота

- `/start` - приветственное сообщение
- `/help` - справка по командам
- `/choose_date` - выбрать дату в календаре
- `/send_events_for_today` - отправить события на выбранную дату

### Веб-интерфейс

Откройте http://localhost:5000 в браузере для управления событиями:

- **Просмотр событий** - поиск и просмотр событий
- **Добавление события** - создание новых событий
- **Управление ссылками** - добавление/удаление ссылок на книги и авторов
- **Редактирование** - изменение существующих событий и ссылок

Подробнее в [WEB_EDITOR_GUIDE.md](WEB_EDITOR_GUIDE.md)

## 🔧 Конфигурация

### Переменные окружения

В файле `.env` (скопируйте из `.env.example`):

- `BOT_TOKEN`: токен бота от @BotFather
- `DB_PATH`: путь к базе данных
- `SEND_HOUR`: час отправки рассылки (0-23)
- `TIMEZONE`: временная зона (по умолчанию Europe/Moscow)

### Время отправки рассылки

Отредактируйте `SEND_HOUR` в `.env`:

```
SEND_HOUR=9  # Рассылка каждый день в 09:00
```

### Ограничение количества книг

В файле `literary_calendar_bot.py` найдите метод `format_event_message` и измените:

```python
for book in books[:6]:  # Максимум 6 обложек в одном сообщении
```

## 📚 Структура проекта

```
├── run_bot.py                      # Точка входа - основной бот с командами
├── literary_calendar_bot.py        # Основная логика бота
├── literary_calendar_bot_config.py # Конфигурация (использует .env)
├── literary_calendar_database.py   # Работа с БД
├── telegram_calendar.py            # Компонент календаря для Telegram
├── web_calendar_editor.py          # Веб-интерфейс на Flask
├── web/                            # UI/статика/роуты веб-редактора
├── services/                       # Сервисы (дайджест, юбилеи и т.п.)
├── clients/                        # Клиенты внешних API (GraphQL и т.п.)
├── bot/                            # Форматирование/утилиты для сообщений
├── literary_events.db              # База данных SQLite (локально; не хранится в git)
├── requirements.txt                # Runtime зависимости
├── requirements-dev.txt            # Dev зависимости (поверх runtime)
├── requirements.lock.txt           # Снимок пинов окружения
├── .env.example                    # Шаблон конфигурации
├── Dockerfile                      # Docker контейнер
├── Procfile                        # Heroku deployment
└── README.md                       # Этот файл
```

## 🐛 Решение проблем

### Бот не отвечает на команды

1. Проверьте, что токен правильный в `.env`
2. Убедитесь, что бот добавлен в группу/чат
3. Проверьте логи: `python run_bot.py` должен выводить сообщения
4. Перезагрузите бота: `Ctrl+C` и снова `python run_bot.py`

### Веб-интерфейс не загружается

1. Убедитесь, что Flask установлен: `pip install -r requirements.txt`
2. Проверьте, что порт 5000 свободен
3. Откройте http://localhost:5000 в браузере
4. Проверьте консоль на ошибки

### База данных повреждена

Удалите `literary_events.db` и создайте заново через веб-интерфейс.

## 🔐 Безопасность (важно)

- Не коммитьте токены/секреты (BOT_TOKEN, private_token и т.п.). Используйте `.env`/переменные окружения.
- `GRAPHQL_ENDPOINT` (адрес API) обычно не является секретом, но если это внутренний контур/стенд — лучше не публиковать его, а хранить только в `.env`.

## 🧩 schema.graphql (нужно ли хранить в репозитории?)

Файл `schema.graphql` полезен для разработки (поиск типов, кодоген, автодополнение), но может раскрывать внутренние возможности API.

- Если API публичный/не чувствительный — можно хранить (и обновлять по мере изменений).
- Если API приватный или схема содержит “внутренние” поля — лучше **не коммитить**. В проекте файл по умолчанию в `.gitignore`.


## 🐳 Deployment

### Docker

```bash
docker build -t literary-bot .
docker run --env BOT_TOKEN=your_token literary-bot
```

### Heroku

```bash
git push heroku main
heroku config:set BOT_TOKEN=your_token
```

## 📖 Дополнительная информация

- [QUICK_START.md](QUICK_START.md) - быстрый старт для разработчиков
- [SETUP_INSTRUCTIONS.md](SETUP_INSTRUCTIONS.md) - подробная инструкция по установке
- [WEB_EDITOR_GUIDE.md](WEB_EDITOR_GUIDE.md) - руководство по веб-интерфейсу

## 📄 Лицензия

Свободное использование для некоммерческих целей.

## 🤝 Вклад

Если нашли ошибки или хотите улучшить бота - создайте issue или pull request!
