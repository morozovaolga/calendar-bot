# 🌐 Веб-приложение для управления литературным календарем

## Описание

Веб-приложение для управления событиями литературного календаря с формой для добавления новых событий.

## Возможности

- ✅ Добавление событий через веб-форму
- ✅ Выбор авторов, тегов, категорий из API
- ✅ Редактирование и удаление событий
- ✅ Просмотр всех событий
- ✅ База данных SQLite для хранения
- ✅ Интеграция с GraphQL API

## Установка

1. **Установите зависимости:**

```bash
pip install -r requirements_web.txt
```

2. **Настройте переменные окружения:**

Создайте файл `.env` или установите переменные:

```bash
export GRAPHQL_ENDPOINT="https://ваш-api-endpoint.com/graphql"
export FLASK_SECRET_KEY="ваш-секретный-ключ"
```

3. **Запустите приложение:**

```bash
python web_app.py
```

4. **Откройте в браузере:**

```
http://localhost:5000
```

## Использование

### Добавление события

1. Нажмите "➕ Добавить событие"
2. Заполните форму:
   - **Название события** - будет выделено жирным в сообщении бота
   - **Краткое описание** - дополнительная информация
   - **Дата события** - когда произошло/произойдет событие
   - **Тип события**:
     - Произвольное событие
     - Связано с автором (выбор из API)
     - Связано с тегом (выбор из API)
     - Связано с категорией (выбор из API)

3. При выборе автора/тега/категории:
   - Выберите из выпадающего списка
   - Данные загружаются автоматически из GraphQL API
   - Бот автоматически создаст ссылку на книги

### Редактирование события

1. Нажмите "✏️ Редактировать" на карточке события
2. Измените нужные поля
3. Сохраните изменения

### Удаление события

1. Нажмите "🗑️ Удалить" на карточке события
2. Подтвердите удаление
3. Событие будет деактивировано (мягкое удаление)

## Структура базы данных

Таблица `events`:
- `id` - уникальный идентификатор
- `title` - название события
- `description` - описание
- `event_date` - дата события
- `event_type` - тип ('author', 'tag', 'category', 'custom')
- `reference_uuid` - UUID автора/тега/категории
- `reference_name` - название для отображения
- `is_active` - активно ли событие
- `created_at` - дата создания
- `updated_at` - дата обновления

## API Endpoints

### Получить события на сегодня
```
GET /api/events/today
```

### Получить список авторов
```
GET /api/authors
```

### Получить список тегов
```
GET /api/tags
```

### Получить список категорий
```
GET /api/categories
```

## Интеграция с ботом

Бот автоматически использует базу данных вместо Яндекс календаря.

Если база данных недоступна, бот fallback на Яндекс календарь.

## Безопасность

⚠️ **Важно:** В продакшене:
1. Измените `FLASK_SECRET_KEY` на случайную строку
2. Используйте HTTPS
3. Добавьте аутентификацию
4. Ограничьте доступ к админ-панели

## Развертывание

### Локально
```bash
python web_app.py
```

### С использованием Gunicorn
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 web_app:app
```

### Docker
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements_web.txt .
RUN pip install -r requirements_web.txt
COPY . .
CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "web_app:app"]
```

## Примеры использования

### Добавление события о дне рождения автора

1. Выберите тип "Связано с автором"
2. Выберите автора из списка
3. Введите название: "Иван Иванов родился в 1900 году"
4. Укажите дату рождения
5. Сохраните

Бот автоматически создаст ссылку на книги этого автора!

### Добавление праздника

1. Выберите тип "Произвольное событие"
2. Введите название: "День библиотек"
3. Добавьте описание
4. Укажите дату
5. Сохраните

## Устранение неполадок

### Ошибка подключения к API

Проверьте переменную окружения `GRAPHQL_ENDPOINT`:
```bash
echo $GRAPHQL_ENDPOINT
```

### База данных не создается

Убедитесь, что есть права на запись в директории:
```bash
chmod 755 .
```

### События не отображаются

Проверьте, что события активны (`is_active = 1`)

---

**Готово! Теперь у вас есть полноценная система управления литературным календарем!** 🎉

