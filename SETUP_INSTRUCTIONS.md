# Инструкции по запуску бота

## ✅ Выполненные исправления

### 1. Безопасность
- ✅ Замаскирован реальный токен в `literary_calendar_bot_config.py` (теперь используются переменные окружения)
- ✅ Все параметры теперь загружаются из окружения с безопасными значениями по умолчанию
- ✅ Токен, если был виден в git, должен быть отозван в @BotFather

### 2. Унификация базы данных
- ✅ Переименован `db_create.py` → `literary_calendar_database.py`
- ✅ Удалена дублирующаяся реализация из `literary_calendar_bot.py`
- ✅ Единая точка импорта: `from literary_calendar_database import LiteraryCalendarDatabase`

### 3. Совместимость с python-telegram-bot v20
- ✅ Обновлен `run_bot.py` для корректного асинхронного запуска
- ✅ Добавлен параметр `allowed_updates=Update.ALL_TYPES` в `start_polling()`
- ✅ Все обработчики и методы совместимы с v20

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей
```powershell
python -m pip install -r requirements.txt
```

### Шаг 2: Получение токена бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### Шаг 3: Настройка переменных окружения (Windows PowerShell)
```powershell
$env:BOT_TOKEN = 'YOUR_ACTUAL_TOKEN_HERE'
$env:DB_PATH = 'literary_events.db'
```

**Альтернатива:** Отредактируйте `literary_calendar_bot_config.py` напрямую:
```python
BOT_TOKEN = 'YOUR_ACTUAL_TOKEN_HERE'  # Замените на реальный токен
```

### Шаг 4: Запуск бота
```powershell
python run_bot.py
```

Ожидаемый вывод:
```
🤖 Бот запущен в режиме polling
📚 Доступные команды:
   /start - Начать работу
   /send_events_for_today - События на сегодня
   /choose_date - Выбрать дату
   /help - Помощь
```

## 🔧 Доступные команды

- `/start` - Приветствие и справка
- `/help` - Подробная справка
- `/send_events_for_today` - Получить события на сегодня
- `/choose_date` - Выбрать дату через календарь

## ⚠️ Важные заметки

### Токен безопасности
Если токен был скомитчен в публичный репозиторий:
1. **Немедленно** отозвите токен в [@BotFather](https://t.me/BotFather)
2. Создайте новый токен
3. Используйте новый токен в переменной окружения

### База данных
- По умолчанию: `literary_events.db` (SQLite)
- Путь можно изменить через `DB_PATH` переменную окружения или в конфиге
- Таблицы создаются автоматически при первом запуске

### GraphQL API
- Убедитесь, что endpoint доступен и возвращает корректные данные
- Проверьте логи при ошибках API (строка с "Ошибка запроса к API")

## 📋 Структура файлов

```
D:\calendar-bot\
├── literary_calendar_bot.py              # Основной класс бота
├── literary_calendar_bot_config.py       # Конфигурация (с env fallback)
├── literary_calendar_database.py         # Единая реализация БД
├── telegram_calendar.py                  # Календарь для выбора даты
├── database.py                           # Альтернативный класс работы с БД (не используется)
├── run_bot.py                            # Точка входа с командами
├── requirements.txt                      # Зависимости
├── README.md                             # Полная документация
└── SETUP_INSTRUCTIONS.md                 # Этот файл
```

## 🐛 Решение проблем

### Ошибка: "Module 'telegram.ext' has no attribute 'ContextTypes'"
Убедитесь, что установлена правильная версия:
```powershell
pip install python-telegram-bot>=20.7
```

### Ошибка: "BOT_TOKEN not found"
Установите переменную окружения:
```powershell
$env:BOT_TOKEN = 'ваш_токен'
```

### Ошибка: "Cannot connect to GraphQL API"
1. Проверьте URL в конфиге
2. Убедитесь, что API доступен (ping / curl)
3. Проверьте параметры запроса в логах

### Ошибка при импорте `literary_calendar_database`
Убедитесь, что файл находится в той же папке или Python path включает эту папку.

## 📚 Дополнительные команды

### Инициализация базы данных вручную
```powershell
python -c "from literary_calendar_database import LiteraryCalendarDatabase; db = LiteraryCalendarDatabase(); print('✅ База данных инициализирована')"
```

### Тестирование конфигурации
```powershell
python -c "import literary_calendar_bot_config as config; print(f'Token: {config.BOT_TOKEN[:10]}...'); print(f'DB: {config.DB_PATH}')"
```

## ✅ Финальная проверка

Перед первым запуском убедитесь:
- [ ] Установлены все зависимости (`pip install -r requirements.txt`)
- [ ] Установлен реальный BOT_TOKEN (не плейсхолдер)
- [ ] Бот добавлен в группу/личный чат с правильными правами
- [ ] Все файлы Python скомпилировались без ошибок (`python -m py_compile *.py`)

---

**Версии:**
- Python: 3.8+
- python-telegram-bot: 20.7+
- httpx: 0.25.0+
- beautifulsoup4: 4.12.0+

**Дата обновления:** 7 декабря 2025 г.
