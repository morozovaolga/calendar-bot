# 📚 Полное руководство по веб-интерфейсу редактора календаря

## 🚀 Быстрый старт

```bash
python web_calendar_editor.py
```

Откройте браузер: **http://localhost:5000**

---

## 📋 Функциональность

### 1️⃣ Вкладка "Просмотр" (View)

**Основные возможности:**
- 👀 Просмотр всех событий в таблице
- 🔍 Поиск по названию события в реальном времени
- 📊 Статистика (всего событий, сегодня, ссылок)
- ✏️ Редактирование события (название, описание)
- 🗑️ Удаление события

**Столбцы таблицы:**

| Столбец | Описание |
|---------|---------|
| 📅 Дата | Дата события (MM-DD) |
| 📝 Название | Название события |
| ✏️ Описание | Первые 50 символов |
| 🔗 Ссылки | **✅ N** (зелёный) = есть ссылки<br/>**⚠️ 0** (оранжевый) = нет (загрузятся автоматически) |
| ⚙️ Действия | ✏️ Редактировать / 🗑️ Удалить |

**Поиск:**
Введите часть названия — таблица отфильтруется мгновенно.

---

### 2️⃣ Вкладка "Добавить" (Add)

**Форма добавления события:**

| Поле | Обязательное | Описание |
|------|:----:|---------|
| День | ✅ | 1-31 |
| Месяц | ✅ | Январь-декабрь |
| Название события | ✅ | "День рождения Пушкина" |
| Тип события | ❌ | Литературное/день рождения/смерти/юбилей/памятная дата |
| Описание | ❌ | Краткая информация |

---

### 3️⃣ Вкладка "Ссылки" (References)

**Управление ссылками на книги, авторов, теги, категории.**

#### Просмотр и удаление:
1. Выберите событие из списка
2. Появится таблица с текущими ссылками
3. Нажмите **🗑️ Удалить** рядом с любой ссылкой

#### Добавление новой ссылки:
1. **Тип ссылки:** author / book / tag / category
2. **Название/Имя** (обязательно): "А.С. Пушкин", "Война и мир"
3. **UUID** (опционально): если известен UUID в каталоге

---

## 🎯 Типичные сценарии

### Редактирование события

1. Вкладка **"Просмотр"** → найдите событие
2. Нажмите **✏️ Редактировать**
3. Измените название и/или описание
4. **💾 Сохранить**

### Удаление события

1. Вкладка **"Просмотр"** → найдите событие
2. Нажмите **🗑️ Удалить**
3. Подтвердите

> ⚠️ Это удалит событие и все его ссылки!

### Привязать книгу к событию

1. Вкладка **"Ссылки"**
2. Выберите событие
3. Тип: **book**, Название: "Война и мир"
4. **➕ Добавить ссылку**

### Привязать автора к событию

1. Вкладка **"Ссылки"**
2. Выберите событие  
3. Тип: **author**, Название: "А.С. Пушкин"
4. **➕ Добавить ссылку**

---

## 💡 Что происходит с обложками

### События со ссылками (✅ зелёный счётчик)
- Бот загружает книги по всем привязанным ссылкам
- Показывает обложки (до 6 штук)

### События без ссылок (⚠️ оранжевый 0)
- Бот автоматически ищет по названию события
- Находит и показывает обложки найденных книг
- **Результат:** Всё работает автоматически!

---

## 📊 Статистика

В верхней части вкладки "Просмотр" показываются:

| Метрика | Значение |
|---------|----------|
| 📚 Всего событий | Количество событий в БД |
| 📅 Событий сегодня | События на текущую дату |
| 🔗 Всего ссылок | Количество всех ссылок в БД |

---

## 💾 Структура БД

**Таблица `events`:**
- `id` — уникальный ID
- `event_date` — формат MM-DD (ежегодное)
- `event_type` — тип события
- `title` — название
- `description` — описание

**Таблица `event_references`:**
- `id` — ID ссылки
- `event_id` — ID события (FK)
- `reference_type` — author/book/tag/category
- `reference_uuid` — UUID в каталоге
- `reference_name` — название для отображения
- `metadata` — доп. данные (обложки, аннотации)

---

## 🔧 API для разработчиков

```
GET  /api/events                      — Все события
POST /api/events                      — Создать событие
GET  /api/events/<id>                 — Событие по ID
PUT  /api/events/<id>                 — Обновить событие
DELETE /api/events/<id>               — Удалить событие

GET  /api/events/<id>/references      — Ссылки события
POST /api/references                  — Добавить ссылку
DELETE /api/references/<id>           — Удалить ссылку
```

---

## ⚙️ Заметки

✅ **Хорошие практики:**
- Добавляйте описания для контекста
- Ссылайтесь на известных авторов
- Для автора добавляйте несколько книг
- Используйте теги для группировки

⚠️ **Важно:**
- События ежегодные (дата MM-DD)
- Поиск обложек работает лучше для популярных авторов
- UUID опционален — система работает и без него

---

## 🐛 Решение проблем

| Проблема | Решение |
|----------|---------|
| Не загружаются события | Обновите F5, проверьте консоль (F12) |
| Ошибка при добавлении | Введите название события (обязательное) |
| Обложки не показываются | События могут быть слишком специфичными |
| Сервер не запускается | Проверьте, свободен ли порт 5000 |

---

**Готово! Теперь вы можете полностью управлять литературным календарём.** 📚✨

## Как пользоваться?

### 1️⃣ **Запуск веб-интерфейса**

```powershell
# Перейдите в папку проекта
cd D:\calendar-bot

# Активируйте виртуальное окружение (если еще не активировано)
.\.venv\Scripts\Activate.ps1

# Запустите веб-интерфейс
python web_calendar_editor.py
```

Откроется приложение на **http://localhost:5000**

---

## 📖 Три основные вкладки

### 📖 **Вкладка "Просмотр"** - Список всех событий

- 🔍 **Поиск** - найдите событие по названию
- ✏️ **Редактировать** - измените название и описание события
- 🗑️ **Удалить** - удалите событие (внимание: необратимо!)

**Статистика сверху:**
- 📅 Всего событий в БД
- 📍 Событий сегодня
- 🔗 Всего ссылок на книги

### ➕ **Вкладка "Добавить"** - Создание нового события

Заполните форму:
1. **День** (1-31)
2. **Месяц** (выпадающее меню)
3. **Название события** (обязательно) - например: "День рождения А.С. Пушкина"
4. **Тип события** - выберите из предложенных
5. **Описание** (опционально)

После нажатия "Добавить событие" оно появится в БД и в списке.

### 🔗 **Вкладка "Ссылки на книги"** - Связь событий с авторами/книгами

1. **Выберите событие** из выпадающего меню
2. **Посмотрите текущие ссылки** - какие авторы и книги уже привязаны
3. **Добавьте новую ссылку:**
   - Тип: Автор, Книга, Тег или Категория
   - Название/Имя - например: "Война и мир" или "А.С. Пушкин"
   - UUID (опционально) - если оставить пусто, сгенерируется автоматически

**Примеры:**
```
Тип: Автор
Название: А.С. Пушкин
UUID: author-pushkin

Тип: Книга
Название: Война и мир
UUID: book-war-peace
```

---

## 🔄 Альтернативные способы редактирования БД

### CSV импорт/экспорт

Можно также использовать скрипты командной строки:

```powershell
# Экспортировать события в CSV
python -c "
from literary_calendar_database import LiteraryCalendarDatabase
db = LiteraryCalendarDatabase()
db.export_to_csv('export.csv')
db.close()
print('✅ Экспортировано в export.csv')
"

# Импортировать события из CSV
python -c "
from literary_calendar_database import LiteraryCalendarDatabase
db = LiteraryCalendarDatabase()
db.import_from_csv('import.csv')
db.close()
print('✅ Импортировано из import.csv')
"
```

### Прямое редактирование через Python

```python
from literary_calendar_database import LiteraryCalendarDatabase

db = LiteraryCalendarDatabase()

# Добавить событие
event_id = db.add_event(
    month=12,
    day=25,
    event_type='день рождения',
    title='День рождения Исаака Ньютона',
    description='Английский физик и математик',
    author_name='',
    book_title='',
    year='1643'
)

# Добавить ссылку на автора
db.add_reference(
    event_id=event_id,
    reference_type='author',
    reference_uuid='author-newton',
    reference_slug='isaac-newton',
    reference_name='Исаак Ньютон',
    priority=1
)

# Добавить ссылку на книгу
db.add_reference(
    event_id=event_id,
    reference_type='book',
    reference_uuid='book-principia',
    reference_slug='principia-mathematica',
    reference_name='Математические начала натуральной философии',
    priority=2,
    metadata={'year': '1687', 'link': 'https://example.com'}
)

db.close()
```

---

## 💾 SQLite браузер (альтернатива веб-интерфейсу)

Вы можете использовать DB Browser for SQLite - бесплатный визуальный редактор:

1. Скачайте: https://sqlitebrowser.org/
2. Откройте файл `literary_events.db`
3. Редактируйте таблицы `events` и `event_references` напрямую

---

## 🎯 Рекомендуемый workflow

### Для быстрого добавления нескольких событий:
1. ➕ Используйте вкладку "Добавить"
2. 🔗 Затем добавьте ссылки на авторов/книги через вкладку "Ссылки"

### Для массового импорта:
1. 📄 Подготовьте CSV файл
2. 🐍 Используйте импорт через Python скрипт

### Для редактирования существующих:
1. 📖 Найдите в вкладке "Просмотр"
2. ✏️ Отредактируйте нужные поля

---

## 📚 Формат данных в БД

### Таблица `events`
```
id              - Уникальный ID события
event_date      - Дата в формате MM-DD (например: 12-25)
event_type      - Тип события (день рождения, смерти, памятная дата и т.д.)
title           - Название события
description     - Описание
author_name     - Имя автора (если применимо)
book_title      - Название книги (если применимо)
year            - Год
```

### Таблица `event_references`
```
id                  - Уникальный ID ссылки
event_id            - ID события (внешний ключ)
reference_type      - Тип (author, book, tag, category, film, article)
reference_uuid      - Уникальный идентификатор ресурса (для API)
reference_slug      - URL-friendly идентификатор
reference_name      - Читаемое имя/название
priority            - Приоритет (1=высокий, 3=низкий)
metadata            - JSON с доп. информацией (обложки, аннотации и т.д.)
```

---

## 🚀 Советы и трюки

### Автоматическое добавление references
```powershell
# Добавить references для событий, где их еще нет
python add_event_references.py
```

### Парсинг нового источника
```powershell
# Спарсить события с веб-сайта и добавить в БД
python parse_calendar_web.py
```

### Проверка БД перед запуском бота
```powershell
python check_readiness.py
```

### Просмотр событий на конкретную дату
```python
from literary_calendar_database import LiteraryCalendarDatabase
from datetime import date

db = LiteraryCalendarDatabase()
events = db.get_events_by_date(12, 25)  # 25 декабря

for event in events:
    print(f"📅 {event['title']}")
    print(f"   {event['description']}")

db.close()
```

---

## ⚠️ Частые ошибки

| Ошибка | Решение |
|--------|---------|
| "PORT 5000 already in use" | Закройте другие приложения или измените порт в `web_calendar_editor.py` |
| Unicode ошибки при вводе | Убедитесь, что кодировка терминала UTF-8 |
| БД заблокирована | Закройте другие приложения, работающие с БД |
| Пусто в вкладке "Просмотр" | Перезагрузите страницу (F5) или переключитесь между вкладками |

---

**Готово! 🎉 Теперь вы можете удобно управлять литературным календарём!**
